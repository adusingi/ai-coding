// French strings for ai-coding.adusingi.com
// TODO(Aimable): translate values below. Keys must match en.js exactly.
// To translate later: open this file alongside en.js, change only the right-hand
// side of each "key:" line. The structure must remain identical.
// Until then, this file mirrors en.js so the site works in fr without missing keys.
window.I18N_FR = JSON.parse(JSON.stringify(window.I18N_EN || {}));
// Once translation begins, replace the line above with a full literal object
// (same shape as window.I18N_EN) and translate every string.
